/*
а) Дублировать последний элемент
  List - O(n), идем по всему списку из n эл-тов
  Vector - O(1), добавляем эл-т в конец
б) Удалить первый элемент
  List - O(1), назначуние указателя на следующий эл-т происходит за O(1)
  Vector - O(n), сдвигаем все эл-ты вправо на 1
в) Вставить элементы равные нулю до и после первого минимального
  List - O(n), поиск минимального за O(n), вставка за O(n)
  Vector - O(n), поиск минимального за O(n), сдвиг всех элементов на 2 вправо + вставка за O(n)
г) Заменить средний элемент на последний чётный
  List - поиск среднего за O(n) + поиск последнего чётного за O(n)
  Vector - аналогично List
*/
